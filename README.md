# WorkTracker

A simple app to track work hours at a CrossFit box.

## Tech Stack
- React Native (Expo)
- SQLite (expo-sqlite)
- NativeWind (Tailwind CSS)

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run the app:
   ```bash
   npx expo start
   ```
